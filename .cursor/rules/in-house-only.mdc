---
description: AmL Diagnostic is an official AmL One shelf listing
alwaysApply: true
---

# AmL Diagnostic is a store app

AmL Diagnostic is on the **AmL One official shelf** with **Android** and
desktop clients. OTA lives at `https://aml.one/diagnostic-ota/`.

```
✅ OFFICIAL_LISTINGS / catalog.ts row `diagnostic`
✅ Store icon `diagnostic.png` and app-info copy
✅ Android package `one.aml.diagnostic` / platform key `diagnosticAndroid`
✅ Frankfurt OTA (`/diagnostic-ota/`) when they ask to publish
❌ Listing Icon Tool
```

`publish` / `build the app` / **put it on the store** is OTA + AmL One deploy
+ `graphify update .` + GitHub. See `publish-workflow.mdc`.
